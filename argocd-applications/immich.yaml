apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-immich
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/PieterjanVL2/argocd.git
    targetRevision: HEAD
    path: immich
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: test-immich
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true