apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-argo-helm
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/PieterjanVL2/argocd.git
    targetRevision: HEAD
    path: helm
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: test-helm
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true